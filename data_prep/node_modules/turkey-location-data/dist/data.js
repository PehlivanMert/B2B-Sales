"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.readTurkeyLocationData = readTurkeyLocationData;
exports.readPlateCodes = readPlateCodes;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
function readTurkeyLocationData() {
    const filePath = path_1.default.join(__dirname, "../data/turkey_location_data.json");
    const rawData = fs_1.default.readFileSync(filePath, "utf8");
    return JSON.parse(rawData);
}
function readPlateCodes() {
    const filePath = path_1.default.join(__dirname, "../data/plate_codes.json");
    const rawData = fs_1.default.readFileSync(filePath, "utf8");
    const data = JSON.parse(rawData);
    return Object.entries(data).reduce((acc, [key, value]) => {
        acc[Number(key)] = value;
        return acc;
    }, {});
}
